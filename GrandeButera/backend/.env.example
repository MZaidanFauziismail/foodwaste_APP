PORT=3000
NODE_ENV=development

# PostgreSQL
DATABASE_URL=postgres://sharebite:sharebite@localhost:5432/sharebite
PG_POOL_MAX=10
PG_IDLE_TIMEOUT_MS=30000
PG_CONNECTION_TIMEOUT_MS=10000
DB_AUTO_MIGRATE=true
SEED_DEMO=true

# JWT
JWT_SECRET=sharebite_super_secret_jwt_key_2026
JWT_EXPIRES_IN=7d

# Maps memakai OpenStreetMap/Nominatim, jadi tidak perlu Google Maps API key.

# ML Service URL (Python Flask or embedded)
ML_SERVICE_URL=http://localhost:5001
